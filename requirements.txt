numpy==1.26.4
scipy==1.12.0
soundfile==0.12.1
librosa==0.10.1
pandas==2.2.1
pyyaml==6.0.1
tqdm==4.66.2
matplotlib==3.8.3
scikit-learn==1.4.1.post1
pytest==8.1.0
imageio-ffmpeg==0.4.9
torch==2.2.0
torchaudio==2.2.0
flask==3.0.2
fastapi==0.104.1
uvicorn==0.24.0.post1
pydantic==2.5.2
python-multipart==0.0.6
pillow==10.1.0
redis==5.0.1
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
```[cite: 9]

---

### Integration Notes for Your Backend
* **AI & Vision Pipeline:** `torch`, `torchaudio`, and `pillow` handle your CNN-based deepfake classification and image tampering checks.
* **Audio & Spectral Analysis:** `librosa`, `soundfile`, and `scipy` enable multi-vector acoustic analysis and vocoder anomaly detection.
* **API Frameworks:** Both `fastapi` and `flask` are included so you can run the primary asynchronous endpoints via FastAPI and supplementary lightweight hooks via Flask if needed.

Would you like to write the database models (`sqlalchemy`) and Redis worker integration next?